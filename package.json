{
  "name": "gestor-de-contenido",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck",
    "cli": "pnpm --filter @gc/cli start",
    "comprobar:aislamiento": "node scripts/comprobar-aislamiento.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.7.0",
    "dotenv": "^16.4.5"
  }
}
